<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="400dp"
    android:layout_height="wrap_content"
    android:background="@drawable/custom_border"
    android:maxWidth="400dp"
    android:orientation="vertical"
    app:layout_maxWidth="400dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/gray_light"
        android:orientation="horizontal"
        android:padding="10dp"
        android:weightSum="9">

        <TextView
            android:id="@+id/miw_image_count_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:drawableStart="@drawable/ic_image_accent"
            android:gravity="center"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/miw_audio_count_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:drawableStart="@drawable/ic_audei_accent"
            android:gravity="center"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/miw_video_count_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:drawableStart="@drawable/ic_video_accent"
            android:gravity="center"
            android:textSize="16sp" />
    </LinearLayout>
</LinearLayout>